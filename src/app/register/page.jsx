import RegisterPageClient from "@/Components/RegisterPageClient";

export const metadata = {
  title: "Register | Docure",
  
};

const RegisterPage = () => {
  
  return <RegisterPageClient></RegisterPageClient>
};

export default RegisterPage;
